# Pre-downloaded .whl files for fully-offline `pip install ./wheels/*.whl --no-index`.
# Populate with: powershell -File scripts/fetch_wheels.ps1
# This dir is intentionally NOT gitignored — wheels are committed so the
# codebase stays installable without network access (USB-air-gap scenario).
